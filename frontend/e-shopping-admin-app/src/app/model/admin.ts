// model class in frontend side (Angular) must be mapped to javaBean class in backend side. JavaBean class mapped to table.
export class Admin {
    constructor(public emailid:string,public password:string){}
}
